---
description: "Plan a modify workflow using the modify-plan artifact"
---

## Purpose

Use this extension command when changing existing behavior in brownfield scope.
It is provided by `itx-brownfield-workflows`, not by upstream core workflow
commands.

## Expected planning artifact

Create or update:

`specs/<active-feature>/modify-plan.md`

Use the template at:

`.specify/templates/modify-plan-template.md`

## Next steps

Continue with the standard flow:
`/speckit.tasks` -> implement -> quality gates.
