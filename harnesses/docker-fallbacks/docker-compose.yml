version: "3.9"

services:
  itx-sandbox:
    image: python:3.12-slim
    container_name: ${ITX_CONTAINER_NAME:?Set ITX_CONTAINER_NAME or use the generated .env file}
    command: ["sleep", "infinity"]
    working_dir: /workspace
    volumes:
      - ../../:/workspace
